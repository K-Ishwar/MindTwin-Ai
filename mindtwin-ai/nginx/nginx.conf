worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Gzip Compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Rate limiting: 100 requests per minute per IP
    limit_req_zone $binary_remote_addr zone=mylimit:1m rate=100r/m;

    map $http_origin $cors_origin {
        default "";
        "~^http://localhost:3000$"  $http_origin;
        "~^http://localhost:5173$"  $http_origin;
        "~^http://localhost:5174$"  $http_origin;
        "~^http://localhost:19000$" $http_origin;
        "~^https://mindtwin\.app$"  $http_origin;
    }

    server {
        listen 80;

        # Rate limiting
        limit_req zone=mylimit burst=20 nodelay;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        
        # CORS Headers
        add_header Access-Control-Allow-Origin $cors_origin always;
        add_header Access-Control-Allow-Credentials "true" always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Accept,Authorization,Cache-Control,Content-Type,DNT,If-Modified-Since,Keep-Alive,Origin,User-Agent,X-Requested-With" always;

        # Preflight response
        if ($request_method = 'OPTIONS') {
            return 204;
        }

        # Proxy settings
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Health Check
        location = /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }

        # Microservices Routes — no trailing slash: NGINX forwards full /api/... path
        # proxy_hide_header strips upstream CORS headers so NGINX's add_header is the only one
        location /api/auth/ {
            proxy_pass http://auth-service:3001;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
        }
        location /api/profile/ {
            proxy_pass http://profile-service:3002;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
            proxy_hide_header Access-Control-Allow-Methods;
            proxy_hide_header Access-Control-Allow-Headers;
        }
        location /api/scheduler/ {
            proxy_pass http://scheduler-service:3003;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
        }
        location /api/quiz/ {
            proxy_pass http://quiz-service:3004;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
        }
        location /api/stress/ {
            proxy_pass http://stress-service:3005;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
        }
        location /api/reward/ {
            proxy_pass http://reward-service:3006;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
        }
        location /api/notifications/ {
            proxy_pass http://notification-service:3007;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
        }
        location /api/ai/ {
            proxy_pass http://ai-engine:8000;
            proxy_hide_header Access-Control-Allow-Origin;
            proxy_hide_header Access-Control-Allow-Credentials;
        }

        # Web Frontend Route (Static Files)
        location / {
            root /usr/share/nginx/html;
            index index.html index.htm;
            try_files $uri $uri/ /index.html;
        }
    }
}
